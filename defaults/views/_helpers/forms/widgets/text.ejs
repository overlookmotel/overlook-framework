<%
// receives (field, fieldName, first)
%><input name="<%= fieldName %>" value="<?= value ?>"<%
if (field.length) { %> maxlength="<%= field.length %>"<% }
if (first) { %> autofocus<% }
%> class="form-control" />