<%
// receives (form)
%><div class="col-sm-offset-2"><%

var submit = form.submit || {};
var text = submit.text || 'Save';
var glyph = submit.glyph || 'ok';
var mode = submit.mode || 'primary';
%><% include ../buttonSubmit %><%

if (!submit || !submit.noCancel) {
	var cancel = form.cancel || {},
		url = cancel.url || './',
		text = cancel.text || 'Cancel',
		glyph = cancel.glyph || 'remove',
		mode = cancel.mode || 'warning';
%> <% include ../buttonLink %><%
}

%></div>
</form>